apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: NodePort
  selector:
    app: {{ .Values.backend.name }}
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: {{ .Values.backend.nodePort }}
