apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smart-inventory.fullname" . }}-test-connection"
  labels:
    {{- include "smart-inventory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smart-inventory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
